## Анализ приложения "Расписание РГСУ"
# Обзор проекта
Приложение для просмотра расписания занятий РГСУ с двумя режимами отображения: календарь и таблица. Данные загружаются из Google Sheets.

Технологический стек:
Next.js 14 (App Router), React 18, TypeScript, shadcn/ui, Tailwind CSS, SWR.

# Предложения по улучшению

## ✅ Выполнено
1. **Кеширование API на сервере**
   - Реализовано: `revalidate = 60` в `/api/schedule`.
2. **Дублирование типа ScheduleItem**
   - Исправлено: Тип унифицирован в `types/schedule.ts`.
3. **Улучшить доступность (a11y)**
   - Частично: Добавлены `aria-label` для основных элементов.
4. **PWA поддержка**
   - Реализовано: Настроен `@ducanh2912/next-pwa`, создан `manifest.json`.
5. **Сохранение фильтров в URL**
   - Реализовано: Синхронизация через `window.history.replaceState` и инициализация из `searchParams` в Server Component.
6. **Улучшить SEO**
   - Реализовано: Динамическая генерация `metadata` в `app/page.tsx` на основе параметров URL.
7. **Неиспользуемые зависимости**
   - Удалено: `qrcode` (лишний, остался `qrcode.react`) и `@heroicons/react`.
8. **UX улучшения для студентов**
   - **Быстрые фильтры**: Реализована панель навигации (Пред/Сегодня/След).
   - **Актуализация**: Подсветка текущего занятия (Badge 'Now', рамка) и дня.
9. **Исправление ошибок и UI**
   - **Vercel Build**: Исправлен конфликт Turbopack/Webpack в `package.json`.
   - **Скролл диалога**: Исправлена проблема с обрезанием контента в окне изменений.
   - **Эстетика**: Улучшен счетчик занятий (абсолютное позиционирование, border-radius).
   - **Индикация изменений**: Добавлена цветовая подсветка (красный/зеленый) для любых изменений (преподаватель, аудитория, предмет).

## 🔴 Высокий приоритет
1. **Виртуализация таблицы**
   - *Текущее состояние*: Используется пагинация.
   - *План*: Заменить пагинацию на бесконечный скролл с `@tanstack/react-virtual` для лучшего UX.
2. **Размер компонента `schedule-view.tsx`**
   - *Проблема*: Файл >950 строк.
   - *План*: Декомпозировать на `ScheduleFilters`, `ScheduleWeekView`, `LessonDialog`.
3. **Улучшение навигации**
   - **Главная страница**: Вместо "Загрузки..." показывать приветственную страницу с описанием сервиса.
   - **Режимы поиска**: Явные переключатели (Группа / Преподаватель / Аудитория) вместо общего поиска.

## 🟡 Средний приоритет

7. **Тёмная тема для PDF**
8. **Интеграция с календарём**
9. **Уведомления о изменениях**

## 📥 Отложенные задачи (Backlog)
- [ ] **Аутентификация для страницы Debug**. Добавить форму входа (логин/пароль из .env) для защиты страницы отладки.

